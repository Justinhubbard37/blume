---
"blume": patch
---

Let Mermaid diagrams take the full content width instead of shrink-wrapping to the SVG's 300px fallback size; small diagrams stay centered and diagrams that set `useMaxWidth: false` still scroll horizontally
