---
"blume": patch
---

Split the sitemap into a sitemap index over numbered `sitemap-N.xml` chunks when a site exceeds 50,000 URLs, the per-file cap search engines enforce. Smaller sites keep the single classic `sitemap.xml`.
