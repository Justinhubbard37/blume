---
"blume": minor
---

Add `seo.og.site` and `seo.og.description` overrides for the generated OG card's footer URL and subtitle, and accept `false` on them (and on `seo.og.logo`) to hide that layer of the card entirely
