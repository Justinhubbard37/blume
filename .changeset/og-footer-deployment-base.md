---
"blume": patch
---

Include `deployment.base` in the generated OG card's footer site text, so a subpath deploy (like a GitHub Pages project site) shows `user.github.io/repo` instead of the bare shared host
