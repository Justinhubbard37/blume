---
"blume": patch
---

Define the `<skill>` placeholder in the blume-migrate skill so the codemod and oxfmt-patch commands resolve to the skill's own directory instead of failing on a literal `<skill>` path
