---
"blume": patch
---

Add `ai.openInChat` to configure the "Open in chat" page action: `false` hides it, and an array of provider keys (`"v0"`, `"chatgpt"`, `"claude"`, `"t3"`, `"scira"`, `"cursor"`) shows just that subset, in order.
