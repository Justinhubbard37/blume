---
"blume": patch
---

Warn at build time, naming the exact package to install, when the configured Ask AI backend's provider SDK isn't installed — instead of failing with an opaque unresolved-import error.
